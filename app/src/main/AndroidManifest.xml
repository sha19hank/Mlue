<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Required on Android 12+ (API 31+) for setExactAndAllowWhileIdle.
         Without this, exact alarms silently fail on API 31+.
         App gracefully falls back to setAndAllowWhileIdle if not granted. -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    


    <application
        android:name=".DailyHabitTrackerApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.DailyHabitTracker">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ReminderReceiver: receives internal AlarmManager broadcasts only.
             exported=false because only our own AlarmManager PendingIntents target it. -->
        <receiver
            android:name=".reminders.ReminderReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.mlue.app.ACTION_HABIT_REMINDER" />
            </intent-filter>
        </receiver>

        <!-- BootReceiver: MUST be exported=true to receive system broadcasts.
             Also handles QUICKBOOT_POWERON for Xiaomi/HTC OEM compatibility.
             MY_PACKAGE_REPLACED restores alarms after the app is updated. -->
        <receiver
            android:name=".reminders.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
